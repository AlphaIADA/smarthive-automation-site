# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme-very-secret

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smarthive

# (Optional) Email provider for passwordless (not used by default)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
